<% provide(:title, 'Create NPI status') %>
<div class="page-header">
  <h3><i class="fa fa-flag"></i> Create New MPI status</h3>
</div>

<div class="row">
    <div class="col-lg-6">
      <div class="panel panel-default">
        <div class="panel-body">        
        <%= bootstrap_form_for(@npiStatus) do |f| %>
          <%= f.text_field :name %>
        </div>
        <div class="panel-footer text-right">
          <%= f.submit "Create MPI status", class: "btn btn-primary" %>
          <%= link_to "Cancel", npi_statuses_path, class: "btn btn-danger" %>
        </div>
        <% end %>
      </div>
  </div>
</div> 